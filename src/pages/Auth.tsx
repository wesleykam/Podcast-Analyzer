import Layout from "../layouts/Layout";

const Auth = () => {
    return (
        <Layout>
            <div>
                <h1>Auth Page</h1>
            </div>
        </Layout>
    );
};

export default Auth;
